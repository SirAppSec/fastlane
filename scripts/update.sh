#!/bin/bash

# Install Python dependencies
pip install -r ../requirements.txt

# Apply any database migrations (if applicable)
# Example: python manage.py migrate

# Restart the Flask server (if running)
# Example: systemctl restart fastlane-scraper
